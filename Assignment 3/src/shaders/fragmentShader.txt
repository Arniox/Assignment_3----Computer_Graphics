#version 130

//gl_Color values are calculated in the vertex shaders and interpolated for this fragment
void main(void){

	//Set textures
	gl_FragColor = texture2D(rtexture, v_texCoord);

	//Set render
	//gl_FragColor = gl_Color;

}